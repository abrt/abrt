# Configuration for the ABRT C/C++ hook on Atomic
# -----------------------------------------------
#
# This config file contains only the default values for ABRT on Project
# Atomic out of the factory. Settings which are not changed here default to
# their preconfigured values. If you wish to override the default settings,
# you can do so here.
#
# For details regarding the contents of this file and the defaults, see
# `man 5 abrt-CCpp.conf`.

JITCoreDumpTracing = no
SaveContainerizedPackageData = yes
StandaloneHook = yes
