EVENT=ask_yes_no_save_result
    echo "ASK_YES_NO_SAVE_RESULT test_yes_no_save_result ABRT ask_yes_no_save_result testing"
    read RES
    if [ "$RES" == "y" ]; then
        echo "test response YES"
    else
        echo "test response NO"
    fi

EVENT=ask_yes_no_yesforever
    echo "ASK_YES_NO_YESFOREVER test_yes_no_yesforever ABRT ask_yes_no_yesforever testing"
    read RES
    if [ "$RES" == "y" ]; then
        echo "test response YES"
    else
        echo "test response NO"
    fi

EVENT=ask_yes_no
    echo "ASK_YES_NO test_yes_no ABRT ask_yes_no testing"
    read RES
    if [ "$RES" == "y" ]; then
        echo "test response YES"
    else
        echo "test response NO"
    fi

EVENT=ask
    echo "ASK test_yes_no ABRT ask_yes_no testing"
    read RES
    echo $RES
