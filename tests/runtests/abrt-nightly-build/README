This tests builds each of the projects listed in $TARGETS.

Each target is built in mock, with matching mock.cfg from mock_configs
directory. When porting to new release, make sure you create proper
mock config and matching if statement in runtest.sh.
