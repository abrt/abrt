This test is using fake_server function to fake communication with bodhi server.
It starts fake bodhi server on port 12345 and then points abrt-bodhi tool to it.

Fake bodhi responses are stored queries/.

To prevent comparison with packages installed in host system, version numbers of
the packages are increased (python-2000, mdadm-3000)
