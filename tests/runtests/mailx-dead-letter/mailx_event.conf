EVENT=notify
	reporter-mailx --notify-only -c /etc/libreport/plugins/mailx.conf
	exit 0

EVENT=notify-dup
	reporter-mailx --notify-only -c /etc/libreport/plugins/mailx.conf
	exit 0

EVENT=report_Mailx      reporter-mailx
