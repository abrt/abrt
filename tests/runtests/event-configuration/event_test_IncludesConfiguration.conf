EVENT=test_IncludesConfiguration
    # Generates a configuration file
    for opt in TEST_OPTION_TEXT TEST_OPTION_BOOL TEST_OPTION_PASSWORD TEST_OPTION_NUMBER  TEST_INCLUDE_OPTION_BOOL TEST_INCLUDE_OPTION_PASSWORD
    do
        echo "$opt = ${!opt}" >> /tmp/test_IncludesConfiguration.conf
    done
