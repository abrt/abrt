EVENT=test_Configuration
    # Generates a configuration file
    for opt in TEST_OPTION_TEXT TEST_OPTION_BOOL TEST_OPTION_PASSWORD TEST_OPTION_NUMBER
    do
        echo "$opt = ${!opt}" >> /tmp/test_Configuration.conf
    done
